# Core (required)
# Note: yfinance 0.2.35 is broken against Yahoo's current API — use a recent release.
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24

# Optional — better ML model (a NumPy fallback is used if absent)
scikit-learn>=1.3 ; extra == "ml"

# Optional — verify TLS via the OS trust store (corporate proxies)
truststore>=0.9 ; extra == "proxy"

# Optional — LLM natural-language analyst
anthropic>=0.39 ; extra == "ai"

# Optional — chart export
matplotlib>=3.7 ; extra == "charts"
